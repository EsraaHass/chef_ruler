import 'package:flutter/cupertino.dart';

class OrderBasket extends StatelessWidget {
  const OrderBasket({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
